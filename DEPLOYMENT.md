# Deployment

This Vite app can be deployed to Vercel with the following settings:

- Build command: `npm run build`
- Output directory: `dist`
- Install command: `npm install`

The site is a concept website and should be clearly presented as such until the business approves the content and imagery.
